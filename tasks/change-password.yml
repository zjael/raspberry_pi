---
- name: Change password
  become: true
  become_user: root
  ansible.builtin.user:
    name: "{{ item.name }}"
    password: "{{ item.password }}"
    update_password: always
  with_items: "{{ account }}"
  no_log: true
